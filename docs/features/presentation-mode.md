# Presentation Mode

Presentation Mode is toggled with `Super + Shift + P` and uses `scripts/system/presentation-mode.sh`.

It reduces interruptions, starts an idle inhibitor when available, relaxes visual effects, and records state under `${XDG_RUNTIME_DIR:-/tmp}/lumina-presentation-mode`. Wlogout moved to `Super + Shift + Escape`.
