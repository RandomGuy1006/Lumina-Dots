# Focus Mode

Focus Mode is toggled with `Super + Shift + F` and uses `scripts/system/focus-mode.sh`.

It reads the focused Hyprland window, moves non-focused windows to `special:focus-hidden`, reduces gaps, and restores windows on the next toggle. Maximize moved to `Super + Ctrl + F`.
