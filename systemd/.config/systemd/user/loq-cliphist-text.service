[Unit]
Description=Cliphist text watcher
PartOf=loq-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/sh -lc 'wl-paste --type text --watch cliphist store'
Restart=always
RestartSec=1

[Install]
WantedBy=loq-session.target

