#!/usr/bin/env bash
# Compatibility alias used by older lumina guides.
set -euo pipefail

SCRIPT_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
ROOT="$(cd "$(dirname "$SCRIPT_PATH")/../../.." && pwd)"
exec "$ROOT/scripts/theme/switch-wallpaper.sh" "$@"
